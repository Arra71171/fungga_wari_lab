# Phase 6 — Plan 02: Fix Public Web Story Archive Page (Inline StoryCard + Skeleton)

## Context
`apps/web/app/stories/page.tsx` has its own inline `StoryCard` and `SkeletonCard`
components (not using the shared one). Both use `aspect-[16/9]`. These need to be
flipped to `aspect-[3/4]`.

---

```xml
<task type="auto">
  <name>Fix story archive page inline StoryCard and SkeletonCard — aspect-[16/9] → aspect-[3/4]</name>
  <files>apps/web/app/stories/page.tsx</files>
  <style-reference>packages/ui/src/components/PortraitFrame.tsx</style-reference>
  <action>
    In `apps/web/app/stories/page.tsx`:

    CHANGE 1 — `StoryCard` cover container (around line 44):
    CURRENT:
    ```tsx
    <div className="relative w-full aspect-[16/9] overflow-hidden bg-muted border-b-2 border-border">
    ```
    CHANGE TO:
    ```tsx
    <div className="relative w-full aspect-[3/4] overflow-hidden bg-muted border-b-2 border-border">
    ```

    CHANGE 2 — `SkeletonCard` skeleton container (around line 134):
    CURRENT:
    ```tsx
    <div className="aspect-[16/9] bg-muted border-b-2 border-border" />
    ```
    CHANGE TO:
    ```tsx
    <div className="aspect-[3/4] bg-muted border-b-2 border-border" />
    ```

    Also update the `sizes` prop on the Image inside StoryCard to reflect portrait
    rendering better (images are narrower in 3-col portrait than in 2-col landscape):
    CURRENT:
    ```tsx
    sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw"
    ```
    KEEP AS-IS (already correct for 3-col grid).

    No other changes needed.
  </action>
  <verify>
    1. `pnpm run typecheck` — must pass
    2. At http://localhost:3001/stories, all story cards must display in portrait
       orientation (taller than wide), forming a natural 3-column grid.
    3. Skeleton cards during loading must also be portrait (taller than wide).
  </verify>
  <done>
    Story archive page at /stories displays portrait story cards and skeletons.
  </done>
</task>
```
