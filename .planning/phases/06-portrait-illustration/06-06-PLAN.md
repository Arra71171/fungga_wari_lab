# Phase 6 — Plan 06: Update AGENTS.md with Section 15 — Illustration & Content Guidelines

## Context
`AGENTS.md` is the law of the project. The portrait-illustration standard must be
codified there so every future AI agent and contributor follows it automatically.
A new Section 15 must be added after the existing Section 14.

---

```xml
<task type="auto">
  <name>Add Section 15 — Illustration & Content Guidelines to AGENTS.md</name>
  <files>AGENTS.md</files>
  <style-reference>AGENTS.md (Section 14 — GitHub & CI/CD Rules)</style-reference>
  <action>
    Append the following section to the end of AGENTS.md, after Section 14:

    ```markdown
    ---

    ## 15. Illustration & Content Guidelines

    > **Activated automatically by the `.agents/skills/portrait-illustration/SKILL.md` skill.**
    > Read that skill file fully before working with any visual asset.

    ### Cultural Theme — Non-Negotiable
    - **ALL** story illustrations, cover images, and scene artwork MUST depict stories
      rooted in **Asian folk traditions**.
    - When the story is Meitei/Kangleipak, characters MUST reflect Meitei heritage:
      traditional phanek/potloi attire, Northeast Indian features, culturally authentic settings.
    - Broader Asian folklore (Mongolian, Japanese, Thai, Cambodian, etc.) is permitted
      for non-Meitei content.
    - ❌ **NEVER** use generic stock photography or Western-styled imagery as story covers.
    - ❌ **NEVER** use Unsplash landscape photography for story cover images.

    ### Orientation — Iron Law
    ```
    ALL STORY ILLUSTRATIONS ARE PORTRAIT. NO EXCEPTIONS.
    ```
    - **Aspect ratio:** `3:4` (width:height) for all story cards, covers, and scene art.
    - **NEVER** use `aspect-video`, `aspect-[16/9]`, or any landscape ratio for story imagery.
    - Use `aspect-[3/4]` in Tailwind for all cover containers.
    - In the story reader, always use the `PortraitFrame` component from
      `@workspace/ui/components/PortraitFrame`.

    ### Visual Style
    - Zen Brutalist folk-art aesthetic: ink-brush textures, high contrast, raw edges,
      ochre/amber dominance (OKLCH hue ~45), ash-tone dark backgrounds.
    - Characters rendered with dignity and cultural accuracy.
    - Primary palette follows design system tokens — no hardcoded colors in illustrations.

    ### Asset Storage
    - Local illustrations: `apps/web/public/illustrations/{story-slug}.png`
    - For Cloudinary (when integrated): follow the same slug-based naming convention.
    - Use `next/image` with `fill` + `object-cover` for all illustration rendering.

    ### Canonical Components
    | Use Case | Component |
    |---|---|
    | Story reader scene illustration | `PortraitFrame` from `@workspace/ui/components/PortraitFrame` |
    | Story archive card cover | `aspect-[3/4]` `div` wrapping `next/image` with `fill` |
    | Shared story card (dashboard + reader) | `StoryCard` from `@workspace/ui/components/StoryCard` |

    ### Forbidden Illustration Patterns
    | Pattern | Instead Use |
    |---|---|
    | `aspect-video` on story image | `aspect-[3/4]` |
    | `aspect-[16/9]` on story image | `aspect-[3/4]` |
    | Unsplash URLs in `coverImageUrl` | Local `/illustrations/{slug}.png` or Cloudinary portrait |
    | Fixed `h-[Npx]` landscape containers | `aspect-[3/4]` fluid container |
    | Raw `<img>` for illustrations | `<Image>` from `next/image` |
    | Landscape-oriented AI image prompts | Always specify "portrait orientation, 3:4 aspect ratio" |
    ```
  </action>
  <verify>
    1. AGENTS.md ends with Section 15 as appended above
    2. No formatting errors — markdown renders correctly
    3. `pnpm run lint` passes (AGENTS.md is not included in lint but verify no side-effects)
  </verify>
  <done>
    AGENTS.md contains Section 15 with full illustration rules.
    Future AI agents will follow the portrait standard automatically.
  </done>
</task>
```
