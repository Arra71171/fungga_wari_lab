<?xml version="1.0" encoding="UTF-8"?>
<phase name="05-public-reader">
  
  <task id="05-01" type="auto">
    <name>Create Public Stories Listing Page</name>
    <files>apps/web/app/stories/page.tsx</files>
    <style-reference>apps/web/app/page.tsx</style-reference>
    <action>
      Create a new "use client" page at apps/web/app/stories/page.tsx.
      Use `useQuery(api.stories.getAll)` from convex/react.
      Import api from `../../../convex/_generated/api`.
      Filter stories to only show those with status === "published" (filter client-side).
      Layout: full-page with Navbar at top, then a grid of story cards.
      Each card: cover image (next/image with fill), title, category badge, language tag, tags.
      Card links to /story/[story.slug] using next/link.
      Empty state: a message "No stories published yet. The archive grows." in font-mono.
      Loading state: skeleton grid using animate-pulse.
      Use only semantic design tokens: bg-background, text-foreground, border-border, text-primary, bg-muted, etc.
      Use shadow-brutal on cards. Cards use Zen Brutalist `rounded-none`.
      Import Navbar from @/components/layout/Navbar.
      Import Button from @workspace/ui/components/button.
      Import Link from next/link, Image from next/image.
      DO NOT import from @workspace/ui/components/card — build the card inline in this file.
    </action>
    <verify>File exists. No TypeScript errors in this file.</verify>
    <done>Page at /stories renders story grid from Convex without errors.</done>
  </task>

  <task id="05-02" type="auto">
    <name>Fix Hero CTA and Navbar Links</name>
    <files>
      apps/web/app/page.tsx
      apps/web/components/layout/Navbar.tsx
    </files>
    <style-reference>apps/web/app/page.tsx</style-reference>
    <action>
      In apps/web/app/page.tsx:
        - Find the "Explore Network" Button (around line 371-378). It currently has no href.
        - Wrap it: add `asChild` prop to the Button, wrap the Button content in a `Link href="/stories"` so clicking navigates to the stories list.
        - Pattern: `&lt;Button size="lg" asChild&gt;&lt;Link href="/stories"&gt;Explore Network ...&lt;/Link&gt;&lt;/Button&gt;`
        
      In apps/web/components/layout/Navbar.tsx:
        - navItems array currently has: { name: "Stories", href: "#stories" }
        - Change that single item's href to "/stories"
        - Leave Platform/Community/Archive as "/#features", "/#community", "/#archive" (not yet built)
    </action>
    <verify>No TypeScript errors. "Explore Network" has href /stories.</verify>
    <done>Clicking "Explore Network" or "Stories" in Navbar navigates to /stories.</done>
  </task>

  <task id="05-03" type="auto">
    <name>Wire StorySidebar to Real Convex Data</name>
    <files>apps/web/components/story/StorySidebar.tsx</files>
    <style-reference>apps/web/components/story/StorySidebar.tsx</style-reference>
    <action>
      The StorySidebar receives `chapters` as a prop (currently `unknown[]`).
      Type the prop properly. A chapter has: _id, title, order, scenes (array of scenes with _id, title, order).
      The component also needs `activeSceneId` (string) and `onSceneSelect` ((sceneId: string) => void) props.
      Replace all hardcoded chapter/scene list items with a `.map()` over the real chapters data.
      Each chapter renders its title. Scenes within each chapter render as clickable items.
      The active scene gets the `border-l-cinematic-accent` highlight style (matching existing active item style).
      Progress bar: show `sceneIndex / totalScenes` as a percentage.
      Keep all existing styling tokens (cinematic-*, brand-ember, etc.) unchanged.
      Keep the bottom branding/settings section unchanged.
    </action>
    <verify>No TypeScript errors. Props are properly typed.</verify>
    <done>Sidebar shows real chapters and scenes from Convex data when storyData loads.</done>
  </task>

  <task id="05-04" type="auto">
    <name>Wire StoryCanvas to Real Scene Content</name>
    <files>apps/web/components/story/StoryCanvas.tsx</files>
    <style-reference>apps/web/components/story/StoryCanvas.tsx</style-reference>
    <action>
      StoryCanvas currently takes `props: { story?: Record&lt;string, unknown&gt; }` and shows hardcoded text.
      Change the prop signature to accept a properly typed scene object:
        type Scene = { title?: string; tiptapContent?: TiptapDoc; illustrationUrl?: string; excerpt?: string }
        type TiptapDoc = { type: "doc"; content?: TiptapNode[] }
        type TiptapNode = { type: string; content?: TiptapNode[]; text?: string; attrs?: Record&lt;string, unknown&gt; }
      
      Add a `TiptapBlockRenderer` function inside the file that recursively renders a TiptapDoc:
        - type "paragraph" → &lt;p&gt; with leading-relaxed styling
        - type "heading" → &lt;h2&gt; or &lt;h3&gt; with font-heading
        - type "blockquote" → &lt;blockquote&gt; with left border in primary color
        - type "text" → span/text node (just render text property)
        - type "hardBreak" → &lt;br /&gt;
        - fallback → null (silently skip unknown types)
      
      Replace the hardcoded story content with:
        - Scene title in the &lt;h1&gt;
        - illustrationUrl: render next/image if available, else keep the cinematic placeholder div
        - TiptapBlockRenderer called with scene.tiptapContent
        - Empty state if no scene: show a centered "Select a scene to begin reading." message
      
      Keep ALL existing styling tokens and layout structure.
    </action>
    <verify>No TypeScript errors. Component renders scene content or graceful empty state.</verify>
    <done>Canvas renders real Tiptap JSON content from the selected scene.</done>
  </task>

  <task id="05-05" type="auto">
    <name>Add Scene Selection State to Story Player Page</name>
    <files>apps/web/app/story/[slug]/page.tsx</files>
    <style-reference>apps/web/app/story/[slug]/page.tsx</style-reference>
    <action>
      Currently the page passes null storyData to components.
      Add scene selection state:
        const [activeSceneId, setActiveSceneId] = React.useState&lt;string | null&gt;(null);
      
      After storyData loads, auto-select the first scene if none is selected:
        Use React.useEffect: if storyData and !activeSceneId, find storyData.chapters[0]?.scenes[0]?._id and setActiveSceneId.
      
      Find the active scene:
        const allScenes = storyData?.chapters.flatMap(c => c.scenes) ?? [];
        const activeScene = allScenes.find(s => s._id === activeSceneId) ?? null;
      
      Pass to StorySidebar: chapters={storyData?.chapters}, activeSceneId, onSceneSelect={setActiveSceneId}
      Pass to StoryCanvas: scene={activeScene}
      
      Fix the "cinematic-bg" className issue: replace `bg-cinematic-bg` and `text-cinematic-text` with `bg-background` and `text-foreground` in the outer wrapper div since cinematic tokens may not be in the Zen Brutalist globals.css. Check existing classes and keep ones that exist (cinematic-* from older config).
      
      Note: params must be awaited in Next.js 15 App Router. Wrap component properly:
        The page is "use client" so params can be accessed as-is (client component).
    </action>
    <verify>No TypeScript errors. Page compiles.</verify>
    <done>Player page selects first scene on load, sidebar clicks change active scene, canvas shows content.</done>
  </task>

  <task id="05-06" type="auto">
    <name>Update Planning STATE.md</name>
    <files>.planning/STATE.md</files>
    <action>
      Rewrite STATE.md to reflect actual current state:
        - Phases 0-4 are ALL COMPLETE
        - Phase 5 (Public Reader) just completed
        - All technologies are integrated
        - List what's actually done
    </action>
    <verify>File is updated with current state.</verify>
    <done>STATE.md reflects reality.</done>
  </task>

</phase>
