// convex/auth.ts has been removed — authentication is now handled by Clerk.
// Convex verifies the Clerk JWT automatically via auth.config.ts.
// Use ctx.auth.getUserIdentity() in queries/mutations to access the authenticated user's identity.
